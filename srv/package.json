{
  "name": "kora-latecut",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "scripts": {
    "build": "cds build/all --clean;",
    "schema": "cds compile ../srv-movies --to sql > src/main/resources/schema.sql; cds compile ../srv-books --to sql >> src/main/resources/schema.sql;",
    "deploy": "cd ..; cds deploy"
  },
  "dependencies": {
    "@sap/cds-dk": "^3.5.1",
    "sqlite3": "^5.0.0"
  },
  "cds": {
    "build": {
      "target": ".",
      "tasks": [
        {
          "for": "java-cf",
          "src": ".",
          "options": {
            "model": [
              "../srv-movies",
              "../srv-movies/db",
              "../srv-books",
              "../srv-books/db"
            ]
          }
        }
      ]
    },
    "odata": {
      "version": "v2"
    }
  }
}
