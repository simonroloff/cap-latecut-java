## Generated mta.yaml based on template version 0.2.0
## appName = kora-suite-cap2
## language=java; multiTenant=false
## approuter=
_schema-version: '3.1'
ID: kora-latecut
version: 1.0.0
description: "Generated by cds-services-archetype"
parameters:
  enable-parallel-deployments: true  
build-parameters:
  before-all:
   - builder: custom
     commands:
      - npm install

modules:
 # --------------------- SERVER MODULE ------------------------
 - name: javacap
 # ------------------------------------------------------------
   type: java
   path: .
   properties:
     EXIT: 1  # required by deploy.js task to terminate 
   build-parameters:
      builder: custom
      commands: [ mvn clean package ]
      build-result: "srv/target/*.[wj]ar"



